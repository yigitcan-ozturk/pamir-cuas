# Gate A acquisition blocker

The validation harness is ready, but the live Figshare repository inventory could not be resolved through the current execution environment.

This is an external acquisition blocker, not a validation result.

No repository file identifiers, download URLs, checksums, SHA-256 values, or real C-FV results have been inferred or fabricated.

## Unblock paths

Either of the following is sufficient:

1. acquire the public TSMS-Drone payload from DOI 10.25452/figshare.plus.30027313 and place the selected source files in the local validation data directory; or
2. provide/export the live Figshare file inventory so the acquisition scripts can resolve repository file URLs.

Once available, the existing pipeline performs repository checksum verification where available, independent SHA-256, MAT contract validation, index-aligned triplet selection, C-FV01 preflight, Evidence Object generation and reproduction recording.
